export default function ({redirect}) {
  // Add the userAgent property to the context
  // if (!localStorage.getItem('organization')) {
  // if (!localStorage.getItem('organization')) {
  //   return redirect('/admin/organizations/company')
  // }
  // if (localStorage.getItem('organization')) {
  //   return redirect('/admin/dashboard')
  // }
}
